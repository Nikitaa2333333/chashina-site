# Сайт Оксаны Чащиной — врач-косметолог, превентивная медицина

Персональный лендинг (одна страница + служебные в планах). Дизайн скопирован
с **integratedbio.com** («биолюминесцентная лаборатория»: тёмно-зелёный канвас,
один лаймовый акцент, единственное начертание 400, mono-подписи) и адаптирован
под бьюти-тематику. Вся типографика и отступы — токенами.

## Стек и запуск

- **Astro 5**, чистый CSS (без Tailwind), ванильный TS в `<script>` секциях.
- Дев-сервер: `npx astro dev --host 127.0.0.1 --port 4331`
  (без явного `--host` порт наружу не отдаётся — правило из глобального CLAUDE.md).
- Открывать: http://127.0.0.1:4331/
- Деплоя пока нет. Прод-сборка: `npx astro build` → `dist/`.

## Файлы

```
Тексты_сайта_Чащина.md   ← ИСХОДНИК КОНТЕНТА: 18 блоков от копирайтера.
                            Куски «[от Оксаны]» ещё не написаны — на сайте там рыба
PROMPTS.md               ← промпты для генерации иллюстраций (стиль, хексы, AR)
SPACING.md               ← как устроен пульт отступов (читать перед правкой сетки)
CLAUDE.md                ← этот файл. После значимых правок дополнять лог внизу
src/
  layouts/Base.astro     ← <head>, favicon, прелоад шрифтов, глобальный типограф
  components/
    Header.astro         ← фикс-меню справа, глассморфизм (blur 16px)
    BtnPair.astro        ← фирменная кнопка: тёмная пилюля + лаймовый чип-стрелка
    ArrowChip.astro      ← отдельный лаймовый квадрат со стрелкой 40×40
  styles/global.css      ← ВСЕ токены (цвета, шрифты, отступы, радиусы) + база
  lib/typo.ts            ← типограф: клеит висячие предлоги неразрывным пробелом
  pages/index.astro      ← вся главная: разметка + стили секций + скрипты сцен
public/
  fonts/                 ← Inter Tight 400 и Roboto Mono 400, сабсеты cyr+lat
  photos/hero.png        ← вырезка Оксаны с прозрачным фоном (хиро)
  photos/web/01–10.webp  ← оптимизированные кадры с приёмов (1000px)
  photos/*.jpg           ← 64 исходника, НЕ подключать напрямую (300–800КБ)
  photos/art/            ← сгенерированные иллюстрации методов (method-01..04.webp)
  video/ripple.mp4       ← фон SMAS-секции (волны, 10с, 720p)
  video/serum.mp4        ← фон «Не с крема» (сыворотка, 8с, 720p)
```

Исходные PNG генераций лежат в корне (`ChatGPT Image*.png`) — источники для
`photos/art/`, в сборку не попадают. Конвертация: `ffmpeg -i in.png -vf scale=1100:-2 out.webp`.

## Дизайн-система

Всё в `src/styles/global.css`, менять токены — не значения в компонентах.

**Цвета:** `--ink #222f30` (тёмный канвас/текст, НЕ чёрный — зелёная нота),
`--lime #cef79e` (единственный акцент: чипы-стрелки, точки тегов, `<mark>`),
`--lime-2 #a7e26e` (hover), `--bone #f7f7f5` (светлый канвас), `--paper #fff`,
`--sea #445e5f` (границы на тёмном, 4-я плашка), `--sage #c9cbbe` (hairline на
светлом), `--tissue #e4e3e3`, `--frost #eee`, `--void #000` (только якоря).

**Типографика:** одно начертание 400 на всём сайте. Иерархия — размером и
отрицательным трекингом (крупнее кегль → сильнее трекинг, до −0.03em).
Sans — Inter Tight (замена Aspekta, у которой нет кириллицы); mono — Roboto Mono
для СЛУЖЕБНОГО: теги секций, навигация, кнопки, подписи — всегда uppercase
через класс `.mono`. Шкала — резиновые токены `--text-hero … --text-caption`.

**Жёсткие правила** (из глобального CLAUDE.md пользователя, зашиты предохранителем в CSS):
- Никакого bold/italic внутри наборного текста. Акцент — только `<mark>` (лаймовая
  подложка), фраза должна влезать в одну строку, 1–2 на секцию.
- Никаких буллитов и разделительных линий в перечислениях — только ритм.
- Текст только контрастный: на тёмном чистый `#fff`, на светлом `--ink`.
  Не приглушать серым/прозрачностью (плейсхолдеры форм — исключение).
- Мера строки токенами в `ch` на самом элементе (`--measure-*`), см. SPACING.md.
- Тени запрещены — глубина цветом поверхности и hairline-границами.
- Лайм не бывает фоном крупных поверхностей и основных кнопок.

**Отступы:** пульт в `:root` (`--page-max 1520`, `--gutter`, `--section-gap` —
это ПОЛОВИНА дыры между секциями). Подробности и ловушки — в SPACING.md.

**Скругления:** кнопки 8, шапка 12, карточки 20, крупные плашки 24, пилюли 9999.

**Фирменные приёмы:** тёмные секции — скруглённые плашки (`border-radius: 24px;
margin-inline: 12px`) на светлом канвасе; тег секции — белая пилюля с лаймовым
квадратиком; кнопка — пара «тёмная пилюля + лаймовый чип-стрелка» (BtnPair);
глассморфизм в шапке и на фото-плашках слайдера; изинг `--ease cubic-bezier(0.16,1,0.3,1)`.

## Секции главной (порядок в index.astro)

1. **Хиро** — скролл-сцена (sticky, трек 260svh): на старте только вырезанное
   фото по центру на светлом фоне; по скроллу «Оксана» выезжает слева, «Чащина»
   справа (за фото), затем снизу проявляются подзаголовок + отстройка + BtnPair.
   Фон — canvas `[data-hero-mol]`: ~46 дрейфующих «молекул» со связями
   (плексус), каждая пятая лаймовая. Останавливается вне вьюпорта.
2. **Подход / Две половины одной работы** — тёмная плашка: колонки
   «01. Снаружи / 02. Изнутри» (без карточек, чистая типографика), вывод с `<mark>`.
3. **Как строится работа** — заголовок «Почему я не начинаю с лифтинга», три
   полноэкранные панели-ступени (лайм/тёмная/серая, edge-to-edge, без радиусов)
   + SVG-линия прогресса, «Что для меня важно» — три редакционные строки
   (заголовок слева, текст справа).
4. **Манифест** — центрированная крупная фраза с круглыми фото-вставками
   размером с букву (`.statement__dot`). Референс: supplement-сайт. Сейчас в
   кругах фото-заглушки, заменить на генерации (PROMPTS.md §8).
5. **Аппаратные методы** — горизонтальный скролл-слайдер `[data-mscroll]`:
   sticky-сцена, 4 вертикальные фото-плашки (art/method-01..04) листаются по
   скроллу, справа градиентное затухание (гаснет к концу). Низ каждой плашки
   «утопает» в стекле: размытая копия фото с маской (`filter`, НЕ backdrop —
   backdrop-filter на движущемся лагает). Мобайл/reduced-motion — нативный свайп.
   После — «Чего я больше не делаю».
6. **SMAS/птоз** — тёмная плашка, текст + фоновое видео ripple.mp4.
7. **Инъекционные методы** — 4 задачи сеткой, закрывашка с `<mark>`.
8. **Зоны, которые выдают возраст** — 3 колонки (шея/подбородок/кисти).
9. **Не с крема** — видео serum.mp4 + текст, кнопка «Читать статьи» (пока #).
10. **Соцсети** — тёмная плашка, 3 строки-ссылки (TG/VK/Дзен) с лайм-ховером.
11. **Пресса** — 6 публикаций (РБК, ТАСС, Газета.ру, Flacon, U Magazine, Beautyhack).
12. **Обо мне** — текст (рыба!) + замыкающая мысль, `<details>` «Образование
    и квалификация» с реальными данными из структуры.
13. **Запись** `#contact` — тёмная плашка с формой: имя, телефон, комментарий,
    чекбокс согласия, submit. **Бэкенда нет** — форма визуальная.
14. **Футер** — тёмный, statement + навигация + соцсети, гигантский вордмарк
    «Чащина», обязательная строка «Имеются противопоказания…».

Все скролл-механики живут в одном `<script>` в index.astro и уважают
`prefers-reduced-motion` (статичное финальное состояние).

## Чего ещё нет (по структуре из Тексты_сайта_Чащина.md)

- **Блок 10 «Домашний уход»** и **11 «Видео» (вертикальные ролики)** — ждут
  материалов от Оксаны.
- **15 «Статьи»** — нужен раздел /articles: список с рубриками, поиск, страница
  статьи. На главной — 3–4 превью. Сейчас кнопка «Читать статьи» ведёт в никуда.
- **16 «Отзывы»** — ссылки на агрегаторы (Яндекс.Здоровье и др.), ссылок пока нет.
- **18 «Контакты»** — телефон/почта/адрес не получены; в футере только соцсети.
- **Бэкенд формы записи** (почта или телеграм-бот) + страница благодарности, 404,
  политика ПД (текст даёт заказчик).
- Мета: OG-теги, sitemap, robots. Деплой и домен. Замена рыбы «[от Оксаны]»
  в методах/зонах/обо мне. Кругляши манифеста → генерации.
- Пройтись по вертикальным отступам плашечных секций (SMAS, соцсети, запись) —
  пользователь отмечал, что внутри много воздуха сверху/снизу (см. лог).

## Лог обновлений

Вести здесь, поднимая новые записи наверх. Формат: дата — что сделано/решено.

- **2026-08-07 (вечер)** — Съёмка в клинике «Атлас» (52 кадра) разложена по сайту
  и собрана в галерею. Исходники — `photos-src/chashina-atlas/` (вне сборки),
  дериваты делает `scripts/build-photos.ps1`: `public/photos/clinic/` 900px для
  плиток и врезок, `clinic/lg/` 1440px для лайтбокса. Кадры с крупными
  логотипами Belotero не берём.
  **Новая секция «Галерея»** между «Обо мне» и «Атласом»: без тега и заголовка,
  только кадры. Десктоп — мозаика edge-to-edge (вертикаль 2 ряда, горизонталь 1),
  телефон — горизонтальная лента 400px с градиентом справа, который гаснет
  в конце прокрутки. Лайтбокс свой, без библиотек: стрелки, Esc, свайп, счётчик.
  **Врезки:** студийный портрет вместо аватарки в «Обо мне», полоса 21:9
  с процедурой в «Инъекционных методах», лайфстайл-кадр в «Домашнем уходе».
  Съёмки НЕ мешаем подряд: клиническая держит рабочие секции, лайфстайл —
  бытовые; в галерее обе, но блоками.
  Спека: `docs/superpowers/specs/2026-08-07-gallery-and-photos-design.md`.

- **2026-08-07** — Мобильная адаптация (пройдена вся страница на 390px):
  хиро на ≤900px больше не скролл-сцена — статичная компоновка (имя лесенкой,
  фото, текст ниже; JS сцены отключён через matchMedia); у свайп-слайдера
  методов спрятан системный скроллбар. Остальные секции легли без правок.
  Пользователь параллельно добавил: блок 10 «Домашний уход» (`.care`, NAOS)
  и секцию «Где я принимаю» (`.clinic`, клиника «Атлас», телефоны/адреса,
  `#contact`) ВМЕСТО формы записи — формы на сайте больше нет, все CTA ведут
  на контакты клиники. Написан этот CLAUDE.md.

- **2026-08-05** — Слайдер методов: убраны номера-чипы; фото-плашки получили
  «стеклянный» низ (размытая копия + маска). Сгенерированы и подключены 4
  иллюстрации методов (art/method-01..04.webp). Манифест перенесён выше — между
  «Как строится работа» и слайдером. Добавлены блоки: Зоны (8), Соцсети (12),
  Обо мне (14, с details-квалификацией), Запись (17, форма `#contact`).
  Футер: statement переписан, «Обо мне» в навигации. Создан PROMPTS.md.
  Отступы: `.mscroll` получил отрицательные margin-block (компенсация пустоты
  100svh-сцены), манифест — `section--tight`. НЕ ЗАКРЫТО: большие внутренние
  отступы тёмных плашек (SMAS/соцсети/запись) — жалоба «много пространства
  снизу и вверху».
- **2026-08-04 (вечер)** — Хиро переделан в светлый без плашки: фото по центру,
  имя выезжает с боков по скроллу, снизу проявляется текст. Фон — canvas-молекулы
  (сетку и светящийся круг пробовали — убрали). Шапка: логотип-плашка удалена,
  меню — глассморфизм. Секция «С чем приходят» удалена целиком (после трёх
  итераций дизайна). «Две половины» — без плашек-карточек, текст колонок ~22px.
  «Что для меня важно» — редакционные строки вместо белых карточек.
  Эксперименты с фото (лента/галерея/вставки в блоки) — всё убрано по просьбе.
  Аппаратные методы превращены в горизонтальный скролл-слайдер, добавлены
  SMAS (ripple.mp4), Инъекции, «Не с крема» (serum.mp4), Пресса. Favicon.
- **2026-08-04 (день)** — Проект переведён с рыбы «Доктор Ливанова» на реальный
  контент Оксаны Чащиной (Тексты_сайта_Чащина.md). Собраны блоки 1–4 + футер.
  Marquee-бегущая строка удалена (не возвращать).
