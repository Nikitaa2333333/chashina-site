# Сайт Оксаны Чащиной — врач-косметолог, превентивная медицина

Персональный лендинг (одна страница + служебные в планах). Дизайн скопирован
с **integratedbio.com** («биолюминесцентная лаборатория»: тёмно-зелёный канвас,
один лаймовый акцент, единственное начертание 400, mono-подписи) и адаптирован
под бьюти-тематику. Вся типографика и отступы — токенами.

## Стек и запуск

- **Astro 5**, чистый CSS (без Tailwind), ванильный TS в `<script>` секциях.
- Дев-сервер: `npx astro dev --host 127.0.0.1 --port 4331`
  (без явного `--host` порт наружу не отдаётся — правило из глобального CLAUDE.md).
- Открывать: http://127.0.0.1:4331/
- Деплоя пока нет. Прод-сборка: `npx astro build` → `dist/`.

## Файлы

```
Тексты_сайта_Чащина.md   ← ИСХОДНИК КОНТЕНТА: 18 блоков от копирайтера.
                            Куски «[от Оксаны]» ещё не написаны — на сайте там рыба
PROMPTS.md               ← промпты для генерации иллюстраций (стиль, хексы, AR)
SPACING.md               ← как устроен пульт отступов (читать перед правкой сетки)
CLAUDE.md                ← этот файл. После значимых правок дополнять лог внизу
docs/тексты-сайта.md     ← выгрузка ВСЕГО текста сайта на сверку с Оксаной.
                            Генерится, руками не править (см. «Тексты на сверку»)
scripts/
  extract-texts.mjs      ← собирает выгрузку из dist/index.html (+ HTML для Google Docs)
  import-telegram.mjs    ← импорт постов канала в блог из выгрузки Telegram Desktop
  lib/gdoc-html.mjs      ← Markdown → HTML: без него Google съедает <mark>
  verify-gdoc.mjs        ← сверка залитого документа с выгрузкой
  build-photos.ps1       ← дериваты фото из photos-src/
src/
  layouts/Base.astro     ← <head>, favicon, прелоад шрифтов, глобальный типограф
  components/
    Header.astro         ← фикс-меню справа, глассморфизм (blur 16px)
    Footer.astro         ← общий футер (главная + блог), вордмарк «Чащина»
    BtnPair.astro        ← фирменная кнопка: тёмная пилюля + лаймовый чип-стрелка
    ArrowChip.astro      ← отдельный лаймовый квадрат со стрелкой 40×40
  styles/global.css      ← ВСЕ токены (цвета, шрифты, отступы, радиусы) + база
  lib/typo.ts            ← типограф: клеит висячие предлоги неразрывным пробелом
  data/telegram-posts.json ← посты блога (генерит import-telegram; сейчас РЫБА)
  pages/index.astro      ← вся главная: разметка + стили секций + скрипты сцен
  pages/blog/            ← блог из телеграм-канала: список + страница поста
public/
  fonts/                 ← Inter Tight 400 и Roboto Mono 400, сабсеты cyr+lat
  photos/hero.png        ← вырезка Оксаны с прозрачным фоном (хиро)
  photos/web/01–10.webp  ← оптимизированные кадры с приёмов (1000px)
  photos/*.jpg           ← 64 исходника, НЕ подключать напрямую (300–800КБ)
  photos/art/            ← сгенерированные иллюстрации методов (method-01..04.webp)
  video/ripple.mp4       ← фон SMAS-секции (волны, 10с, 720p)
  video/serum.mp4        ← фон «Не с крема» (сыворотка, 8с, 720p)
```

Исходные PNG генераций лежат в корне (`ChatGPT Image*.png`) — источники для
`photos/art/`, в сборку не попадают. Конвертация: `ffmpeg -i in.png -vf scale=1100:-2 out.webp`.

## Дизайн-система

Всё в `src/styles/global.css`, менять токены — не значения в компонентах.

**Цвета:** `--ink #222f30` (тёмный канвас/текст, НЕ чёрный — зелёная нота),
`--lime #cef79e` (единственный акцент: чипы-стрелки, точки тегов, `<mark>`),
`--lime-2 #a7e26e` (hover), `--bone #f7f7f5` (светлый канвас), `--paper #fff`,
`--sea #445e5f` (границы на тёмном, 4-я плашка), `--sage #c9cbbe` (hairline на
светлом), `--tissue #e4e3e3`, `--frost #eee`, `--void #000` (только якоря).

**Типографика:** одно начертание 400 на всём сайте. Иерархия — размером и
отрицательным трекингом (крупнее кегль → сильнее трекинг, до −0.03em).
Sans — Inter Tight (замена Aspekta, у которой нет кириллицы); mono — Roboto Mono
для СЛУЖЕБНОГО: теги секций, навигация, кнопки, подписи — всегда uppercase
через класс `.mono`. Шкала — резиновые токены `--text-hero … --text-caption`.

**Жёсткие правила** (из глобального CLAUDE.md пользователя, зашиты предохранителем в CSS):
- Никакого bold/italic внутри наборного текста. Акцент — только `<mark>` (лаймовая
  подложка), фраза должна влезать в одну строку, 1–2 на секцию.
- Никаких буллитов и разделительных линий в перечислениях — только ритм.
- Текст только контрастный: на тёмном чистый `#fff`, на светлом `--ink`.
  Не приглушать серым/прозрачностью (плейсхолдеры форм — исключение).
- Мера строки токенами в `ch` на самом элементе (`--measure-*`), см. SPACING.md.
- Тени запрещены — глубина цветом поверхности и hairline-границами.
- Лайм не бывает фоном крупных поверхностей и основных кнопок.

**Отступы:** пульт в `:root` (`--page-max 1520`, `--gutter`, `--section-gap` —
это ПОЛОВИНА дыры между секциями). Подробности и ловушки — в SPACING.md.

**Скругления:** кнопки 8, шапка 12, карточки 20, крупные плашки 24, пилюли 9999.

**Фирменные приёмы:** тёмные секции — скруглённые плашки (`border-radius: 24px;
margin-inline: 12px`) на светлом канвасе; тег секции — белая пилюля с лаймовым
квадратиком; кнопка — пара «тёмная пилюля + лаймовый чип-стрелка» (BtnPair);
глассморфизм в шапке и на фото-плашках слайдера; изинг `--ease cubic-bezier(0.16,1,0.3,1)`.

## Секции главной (порядок в index.astro)

1. **Хиро** — скролл-сцена (sticky, трек 260svh): на старте только вырезанное
   фото по центру на светлом фоне; по скроллу «Оксана» выезжает слева, «Чащина»
   справа (за фото), затем снизу проявляются подзаголовок + отстройка + BtnPair.
   Фон — canvas `[data-hero-mol]`: ~46 дрейфующих «молекул» со связями
   (плексус), каждая пятая лаймовая. Останавливается вне вьюпорта.
2. **Подход / Две половины одной работы** — тёмная плашка: колонки
   «01. Снаружи / 02. Изнутри» (без карточек, чистая типографика), вывод с `<mark>`.
3. **Как строится работа** — заголовок «Почему я не начинаю с лифтинга», три
   полноэкранные панели-ступени (лайм/тёмная/серая, edge-to-edge, без радиусов)
   + SVG-линия прогресса, «Что для меня важно» — три редакционные строки
   (заголовок слева, текст справа).
4. **Манифест** — центрированная крупная фраза с круглыми фото-вставками
   размером с букву (`.statement__dot`). Референс: supplement-сайт. Сейчас в
   кругах фото-заглушки, заменить на генерации (PROMPTS.md §8).
5. **Аппаратные методы** — горизонтальный скролл-слайдер `[data-mscroll]`:
   sticky-сцена, 4 вертикальные фото-плашки (art/method-01..04) листаются по
   скроллу, справа градиентное затухание (гаснет к концу). Низ каждой плашки
   «утопает» в стекле: размытая копия фото с маской (`filter`, НЕ backdrop —
   backdrop-filter на движущемся лагает). Мобайл/reduced-motion — нативный свайп.
   После — «Чего я больше не делаю».
6. **SMAS/птоз** — тёмная плашка, текст + фоновое видео ripple.mp4.
7. **Инъекционные методы** — 4 задачи сеткой, закрывашка с `<mark>`.
8. **Зоны, которые выдают возраст** — 3 колонки (шея/подбородок/кисти).
9. **Не с крема** — видео serum.mp4 + текст, кнопка «Читать статьи» (пока #).
10. **Соцсети** — тёмная плашка, 3 строки-ссылки (TG/VK/Дзен) с лайм-ховером.
11. **Пресса** — 6 публикаций (РБК, ТАСС, Газета.ру, Flacon, U Magazine, Beautyhack).
12. **Обо мне** — текст (рыба!) + замыкающая мысль, `<details>` «Образование
    и квалификация» с реальными данными из структуры.
13. **Запись** `#contact` — тёмная плашка с формой: имя, телефон, комментарий,
    чекбокс согласия, submit. **Бэкенда нет** — форма визуальная.
14. **Футер** — тёмный, statement + навигация + соцсети, гигантский вордмарк
    «Чащина», обязательная строка «Имеются противопоказания…».

Все скролл-механики живут в одном `<script>` в index.astro и уважают
`prefers-reduced-motion` (статичное финальное состояние).

## Блог из телеграм-канала (/blog/)

Отдельная вкладка (решение заказчика: пока НЕ смешивать с будущими «Статьями»,
объединить можно позже). Список — `/blog/`, пост — `/blog/<slug>/`. Ссылки
«Блог» — в шапке и футере; все якоря в них теперь `/#…`, чтобы работали
с подстраниц. Футер вынесен в `components/Footer.astro`.

**Данные:** `src/data/telegram-posts.json`. Сейчас там РЫБА
(`placeholder: true` — на странице списка видна пометка). Реальные посты
заливает `npm run tg` (= `scripts/import-telegram.mjs`) из выгрузки
Telegram Desktop: папку с `result.json` и `photos/` положить в
`telegram-src/` (в git не идёт). Скрипт перепишет JSON целиком и разложит
фото в `public/photos/tg/` (webp ≤1200px через sharp из зависимостей Astro).

**Правила отбора постов** (зашиты в скрипт, ручки — флагом `--min-chars`):
- только информационные: короче 180 знаков не берём (анонсы, поздравления);
- опросы и сервисные сообщения — мимо;
- пост, где главное медиа — видео/кружок/гифка, не берём целиком;
- фото-альбом собирается в карусель (в выгрузке это сообщения без текста
  следом за постом); видео-участник альбома выбрасывается, пост остаётся;
- жирный/курсив телеграма намеренно плющится в обычный текст (правило одного
  начертания), ссылки сохраняются, хэштеги выбрасываются.

**Вёрстка:** список — сетка карточек (пост без фото — тёмная плашка для
ритма, у каруселей стеклянный счётчик «N фото»); страница поста — узкая
колонка, карусель на scroll-snap со стрелками-чипами и счётчиком (свайп
и reduced-motion уважаются), внизу ссылка на пост в канале + BtnPair
на `/#contact`. Выгрузка текстов (`npm run texts`) блог НЕ трогает —
она читает только dist/index.html; сверка постов с Оксаной — прямо
по каналу, источник правды там.

## Тексты сайта на сверку (пайплайн перенесён из ktm-site)

Оксана вычитывает контент не в вёрстке, а в Google-документе. Поэтому весь
видимый текст страницы дублируется выгрузкой `docs/тексты-сайта.md`.

- Генерация: **`npm run texts`** (= `astro build` + `scripts/extract-texts.mjs`);
  текст берётся из собранного `dist/index.html`, то есть дословно совпадает с сайтом.
- **Правило: поменял текст на странице → сразу перегенерировать выгрузку и
  закоммитить вместе с правкой.** Выгрузка не должна отставать от кода.
- Файл выгрузки руками НЕ редактировать — источник правды по тексту всегда код.
- Формат: каждая секция — раздел «NN. Тег — Заголовок» с меткой `*(секция: #id)*`,
  по ней правка клиента находит место в вёрстке. `<mark>` сохраняется тегом;
  `*(кнопка)*`, `*(меню)*`, `*(поле: …)*` — служебный курсив.
- Секция без своего тега и заголовка (`.steps`, `.mscroll`) приклеивается к
  предыдущей — своим разделом она читается обрывком. Список секций и запасные
  названия — в `SECTION_NAMES` внутри скрипта; незнакомая секция печатает
  предупреждение, а не молча теряет название.

### Заливка в Google Docs

Рядом с `.md` скрипт кладёт `docs/тексты-сайта.html` (в git не хранится).

- **Заливать в Drive нужно именно HTML** (`contentMimeType: text/html`). Markdown
  Drive тоже принимает, но выбрасывает `<mark>` — фирменная заливка теряется.
  Через HTML доезжает всё: маркер, таблицы, уровни заголовков, служебный курсив.
- **После заливки — обязательная сверка**: вычитать документ обратно, положить
  текст файлом и прогнать `node scripts/verify-gdoc.mjs <файл>`. Ноль пропаж =
  документ совпал дословно. Это не формальность: заливка **рвётся на середине
  незаметно** — документ выглядит целым, а половины текста нет.
- Обновления заливаются НОВЫМ документом (у Drive-коннектора нет апдейта файла) —
  старый лучше удалить руками, чтобы у Оксаны не было двух версий.

## Чего ещё нет (по структуре из Тексты_сайта_Чащина.md)

- **Блок 10 «Домашний уход»** и **11 «Видео» (вертикальные ролики)** — ждут
  материалов от Оксаны.
- **15 «Статьи»** — нужен раздел /articles: список с рубриками, поиск, страница
  статьи. На главной — 3–4 превью. Кнопки «Читать статьи» пока ведут в /blog/.
  (Не путать с /blog/ — там посты телеграм-канала, решили держать отдельно.)
- **Битые ссылки в публикациях** — 13 материалов не открылись на прогоне
  (`data/press-dead.tsv`), их нет на сайте. Показать Оксане: часть можно
  заменить рабочими адресами, часть удалить из `data/press-raw.tsv`.
- **Блог**: реальная выгрузка залита, пагинация есть (см. лог 2026-08-24).
- **16 «Отзывы»** — сделано блоком на главной (см. лог 2026-08-26 (3)); осталось
  периодически сверять цифры с площадками (данные и правило — в комментарии
  у `reviewCards` в index.astro).
- **18 «Контакты»** — телефон/почта/адрес не получены; в футере только соцсети.
- **Бэкенд формы записи** (почта или телеграм-бот) + страница благодарности, 404,
  политика ПД (текст даёт заказчик).
- Мета: OG-теги, sitemap, robots. Деплой и домен. Замена рыбы «[от Оксаны]»
  в методах/зонах/обо мне. Кругляши манифеста → генерации.
- Пройтись по вертикальным отступам плашечных секций (SMAS, соцсети, запись) —
  пользователь отмечал, что внутри много воздуха сверху/снизу (см. лог).

## Лог обновлений

Вести здесь, поднимая новые записи наверх. Формат: дата — что сделано/решено.

- **2026-09-02** — **Сайт выехал на хостинг Спринтхост, автодеплой работает.**
  Аккаунт `a1306629`, сервер `141.8.192.25`. `.github/workflows/deploy.yml`:
  пуш в `main` → `npm ci` → `astro build` → `rsync -az --delete dist/` в
  `public_html`. Ходит по отдельному ed25519-ключу (личный SSH не
  переиспользуем), пять секретов в репозитории: `SSH_HOST`, `SSH_USER`,
  `SSH_PORT`, `DEPLOY_PATH`, `SSH_PRIVATE_KEY`. Пароль аккаунта нужен был
  только на установку ключа, в код и историю не попадал.
  `DEPLOY_PATH` = `/home/a1306629/domains/chashchina.com/public_html/`.
  Запасной вход — техадрес `a1306629.xsph.ru` (сборка там тоже лежит).
  Рядом `ssh-check.yml` — диагностический workflow (Actions → Run workflow):
  показывает `~/domains` и содержимое `public_html`. **Нужен потому, что
  локальный SSH хостинг режет**: после нескольких подключений подряд сервер
  отдаёт баннер и рвёт сессию (`Error reading SSH protocol banner`, порт при
  этом открыт). Раннер GitHub ходит со своего IP и по ключу — проходит.
  **Ловушка с опознанием хостинга:** сервер представляется `mjodgiver.from.sh`,
  техадрес аккаунта — `a1306629.xsph.ru`; и `from.sh`, и `xsph.ru` — общая
  инфраструктура холдинга, по ним хостинг определяется НЕВЕРНО (я так принял
  Спринтхост за SpaceWeb). Верный признак — подсеть: `141.8.192.x` это
  Спринтхост (`ns3.sprinthost.net` = `141.8.192.118`), а `77.222.x` — SpaceWeb.
  **Домен `chashchina.com` переехал** (регистратор reg.ru, оплачен до
  07.08.2027). Было: NS `ns1/ns2.spaceweb.ru`, A `77.222.40.47`, старый сайт
  «Дерматокосметолог Оксана Чащина 2024» на PHP 7.1. Стало: на reg.ru
  прописаны `ns1.sprinthost.ru`, `ns2.sprinthost.ru`, `ns3.sprinthost.net`,
  `ns4.sprinthost.net` (3 и 4 — зона **.net**, не .ru), домен добавлен в
  панель к `a1306629`, по `chashchina.com` и `www` отдаётся новый сайт.
  **Порядок здесь принципиален: сначала домен в панель, только потом NS.**
  Между сменой NS и заведением домена зоны не существует ни у кого —
  Спринтхост на запрос отвечает `Server failed`, и домен ложится целиком
  (у нас это окно было около часа, спасло то, что резолверы держали кеш).
  **НЕ ЗАКРЫТО: SSL.** На 443 висит самоподписанный сертификат
  (`CERTIFICATE_VERIFY_FAILED: self-signed`), https не открывается —
  включить бесплатный Let's Encrypt в панели Спринтхоста, на домен и www.

- **2026-09-01** — Обложки-иллюстрации доведены до **32 из 69** публикаций
  (было 7). Все шесть карточек, названных копирайтером для главной, закрыты
  своими кадрами. Осталось три несгенерированных: `karotin-molekula` (НТВ,
  абрикосы), `dga-avtozagar` (НТВ, автозагар), `antioksidant-radikal`
  (Woman's Day, рацион) — на них не хватило 15 ₽, сцены написаны и лежат
  в `presets/chashina/scenes/`, догоняются прогоном без аргументов.
  По деньгам: 150 ₽ = ~31 кадр; **фактически вышло меньше**, потому что
  часть генераций оплачивалась, но не доезжала — отдача с `s3.polza.ai`
  рвалась по таймауту, и кадр терялся вместе с деньгами. Вылечено в
  `image-styler/run.py`: скачивание результата теперь три попытки с
  таймаутом 180 сек вместо одного 60-секундного запроса (на прогоне
  `telomery` доехал только с третьей).
  Правка вёрстки: обложка не доставала до краёв плашки — глобальное
  `img { max-width: 100% }` срезало `width: calc(100% + 40px)` обратно до
  ширины контента. В `.pcard__cover` добавлен `max-width: none`.

- **2026-08-31** — Свои иллюстрации вместо чужих обложек в «Публикациях».
  Генерирует конвейер `image-styler` (лежит ВНЕ репозитория:
  `C:\Users\User\Downloads\test\image-styler`, ключ polza.ai в его `.env`),
  namespace проекта — `presets/chashina/`:
  - `presets/chashina/_base-style.md` — стиль, снятый с четвёрки `photos/art/
    method-01..04`: тёмно-зелёный канвас `#222f30`, единственный акцент — лайм
    `#cef79e`, макро с реальной глубиной резкости, материалы стекло/гель/вода.
    **Две правки, без которых модель врёт:** лайм уезжает в неоновый
    лазерно-зелёный, а сам объект красится в зелёный целиком. В стиле для этого
    отдельный раздел «Colour discipline»: акцент пастельный, объект бесцветный,
    зелёное — только фон и свет;
  - **узнаваемость обязательна.** Первый заход был абстрактный (капли, круги,
    рябь) — заказчик забраковал: «ни о чём не говорит, всё одинаковое». Теперь
    каждый кадр — конкретный научный объект по теме статьи: тройная спираль
    коллагена, молекула кортизола, фокус ультразвука, нейрон с синапсом, ДНК,
    сетка гиалуронки, мицеллы, гранулы меланина, кутикула волоса под SEM,
    гликационные сшивки, лазерный тракт, LED-массив, осмос через мембрану;
  - 24 сцены разложены в `presets/chashina/scenes/`, прогон —
    `image-styler/gen-chashina.ps1` (без аргументов догоняет то, чего нет
    в `output/chashina/`). **Кадр стоит 4.80 ₽**; на прогоне 31.08 баланс
    кончился на девятом — готовы 7, остальные 17 ждут пополнения.
  Пайплайн на стороне сайта: `data/press-art.tsv` (slug → url публикации,
  пишет скрипт раскладки сцен) → `node scripts/press-art.mjs` (png из
  `output/chashina` → webp 1000px в `public/press/art/`) → `node
  scripts/press-build.mjs` (подменяет обложку издания на нашу; строка карты
  без готового webp игнорируется с предупреждением). Обложки видны только
  на `/press` — на главной карточки прессы текстовые.

- **2026-09-01** — **Правки Оксаны из Google-документа** («Оксана Чащина —
  тексты сайта», правлен 01.09) перенесены в вёрстку. Документ был выгрузкой
  от 07.08, поэтому сверял по секциям, а не по номерам разделов:
  - **хиро**: «врач-физиотерапевт» в регалиях, «Практика с 2003 года» вместо
    «22 года практики»; лид переписан целиком — про longevity-подход вместо
    «аппарат сам по себе не решает ничего»;
  - **Подход**: заголовок «Две половины одной работы» → «Интегративный взгляд
    косметолога»; обе колонки и вывод переписаны (в «Изнутри» теперь
    «метаболические изменения… можно лечить и восстанавливать», вывод —
    «не с отдельной жалобой пациента в долгосрочной перспективе»);
  - **ступени**: «Разобраться / Подготовить ткани / Подтянуть и закрепить» →
    «Диагностика / Подготовка тканей / Стимуляция и лифтинг», тексты новые.
    Каждая ступень теперь 2–3 абзаца, поэтому `steps[].text` стал массивом
    абзацев (было одной строкой) — рендер и отступ `.steps__text + .steps__text`.
  - В документе остался раздел «05. Принципы», удалённый с сайта 07.08, —
    НЕ возвращали: это хвост старой выгрузки, а не правка.
  Заголовок `/press` «Где меня спрашивают» заменён на **«Мои статьи в СМИ»**
  (заказчик: «не надо выдумывать велосипед»), лид переписан — про то, что
  издания приходят с вопросами, а через ответы читатель узнаёт о коже больше.

- **2026-08-26 (5)** — Разбор претензий к разделу «Публикации»:
  - **цитаты чистятся** в `press-build.mjs`. Раньше в карточку попадала
    подводка журналиста — «Известный врач-косметолог, заместитель главного
    врача… Оксана Чащина». Теперь режем вводную с именем, берём речь внутри
    кавычек, выбрасываем то, что начинается с регалий, и **всё, где осталась
    фамилия**: с фамилией это пересказ, а на своём сайте она в третьем лице
    читается дико. Было 20 «цитат», осталось 14 настоящих;
  - **мусорные заголовки** заменяются описанием из `press-raw.tsv`: у радио
    со страницы приезжало «слушать эфир бесплатно», у телепрограмм —
    навигация «Живая еда / Выпуски / …»;
  - `nbcdevelopment.ru` выброшен совсем — это карточка врача, не публикация.
    Отсюда 69 материалов вместо 70, изданий 23;
  - **фильтры сняты** (по типу и изданию) — список читают лентой; порядок
    теперь строго по свежести, чередование видов карточек убрано;
  - карточки (и на `/press`, и на главной) получили hairline-границу: белое
    на бежевом сливалось с канвасом; ховер — лаймовая рамка и лёгкая подложка,
    без неё плашка не читалась как кликабельная;
  - `.crumb` — общий стиль возврата «← На главную» в `global.css`, стоит
    на `/press` и `/blog/` (на странице поста он уже был).

- **2026-08-26 (4)** — Разграничены две двери, их постоянно путают:
  **«Блог» = свои тексты из телеграм-канала** (`/blog/`, вход — секция
  «Не с крема», кнопка «Читать блог»), **«Публикации» = что обо мне пишут
  в СМИ** (`/press/`, вход — секция «Пресса», тег «Обо мне пишут», кнопка
  «Все публикации в СМИ», карточки уводят на сайты изданий). Обе теперь
  в шапке отдельными пунктами; при ≤900px пилюли поджимаются полями —
  семь штук в ряд у нижней границы десктопа (761px) иначе не влезают.

- **2026-08-26 (3)** — **Влита ветка `press-archive`** — раздел «Публикации»,
  сделанный в другой сессии и потерянный из виду: она отошла от `2a43292`
  (до блога), поэтому у нас её не было, а в ней не было блога. После merge
  живут оба. Пришло: страница `/press` (фильтры по типу и изданию, карточки
  трёх видов — full/card/line в зависимости от того, есть ли обложка и прямая
  речь), `src/data/press.json` (70 живых публикаций), 41 обложка в
  `public/press/`, скрипты `press-audit.mjs` → `press-build.mjs` →
  `press-covers.mjs`, хелпер `src/lib/asset.ts`.
  Пайплайн: `data/press-raw.tsv` (описание + ссылка, руками) → аудит →
  `data/press-audit.json` (в git НЕ идёт) → сборка → `src/data/press.json`
  (в git идёт) + `data/press-dead.tsv` на сверку с Оксаной.
  Разрешено при слиянии: шапка — наша (с «Блогом», якоря `/#…`); на `/press`
  добавлен наш `Footer`; в футере «Пресса» → «Публикации в СМИ» на `/press/`.
  Шесть карточек на главной теперь берутся по списку копирайтера (РБК, ТАСС,
  Газета.ру, Flacon, U Magazine, Beautyhack) — прежняя сортировка «свежие
  с цитатой» давала четыре Beauty Insider подряд без РБК и ТАСС.
  Мой ручной массив ссылок в index.astro не нужен — данные из press.json.

- **2026-08-26 (3)** — Секция «Отзывы» (#reviews) на главной, между прессой
  и «Обо мне». Тексты отзывов с площадок НЕ копируем: у текста автор-пациент
  и площадка (правила запрещают перепечатку), плюс благодарности пациентов
  на сайте врача — риск по ст. 24 ФЗ «О рекламе». Поэтому в блоке только
  цифры-факты и ссылки на первоисточники: карточки ПроДокторов (20 отзывов)
  и Яндекс Здоровье (95% рекомендуют, 21 отзыв) + тёмная карточка «Ещё
  площадки» (НаПоправку, СберЗдоровье, ДОКТУ — без цифр: там 1–7 отзывов,
  крупная цифра при таком счётчике выглядит враньём). Цифры проверены
  2026-08-26, при правках сверять (устаревший рейтинг = недостоверная
  реклама). «Отзывы» добавлены в навигацию футера; секция знакома
  extract-texts (SECTION_NAMES).

- **2026-08-26 (2)** — Пресса и блог разведены. Пробовали вести карточки СМИ
  на посты блога по теме — так нельзя: под шапкой «ТАСС» обязан открываться
  ТАСС, иначе карточка врёт. Теперь все 6 карточек ведут на первоисточники
  (target=_blank), ссылки живые.
  **Найден аудит прессы, потерянный вне git**: он лежал в старой копии
  проекта `Documents\chashina-site\data\` и в этот репозиторий не попадал.
  Перенесён: `src/data/press-audit.json` (84 публикации 2015–2026 — url,
  издание, заголовок, описание, обложка, год, проверка живости: 70 живых,
  14 битых) + `docs/press-brief.tsv`. Список изданий из текстов копирайтера
  подтверждён данными: РБК, ТАСС, АиФ, Cosmopolitan, Harper's Bazaar,
  Wonderzine, The Blueprint — все реальные. Топ по количеству: U Magazine 12,
  Beauty Insider 11, АиФ 6, The Challenger 6.
  На главной по-прежнему 6 карточек — из аудита можно собрать страницу
  /press со всеми (в исходнике копирайтера так и задумано).
  Приглашение в телеграм-канал живёт в секции «Не с крема» (там же кнопка
  «Читать статьи» → `/blog/`, раньше вела на `#`), в прессе его нет:
  пресса — чужие издания, блог — свои тексты.
  Страница поста: наверху «← На главную», возврат «← Все статьи блога» —
  внизу, после кнопки записи (наверху он перехватывал читателя до текста).

- **2026-08-26** — Список блога: masonry на CSS-колонках вместо grid — карточка
  ровно под свой контент, высоты в ряду не выравниваются (жалоба: тёмная
  карточка растягивалась под соседку с фото). Порядок чтения — вниз по колонке.
  Импортёр: отсев «движ-постов», живущих только в телеграме (квизы, розыгрыши,
  «любимая рубрика — присылайте косметичку», навигация по каналу, поздравления,
  ссылки на ВК/MAX) — только ОДНОЗНАЧНЫЕ стоп-слова ENGAGEMENT_STRONG; мягкие
  пары (приветствие + «напоминаю») пробовали — режут нормальные посты (пилинг
  Peach Peel, разбор фиброза). Штучный добор — scripts/tg-exclude.json (id
  постов). Скрипт печатает список отсеянного для проверки глазами.
  Итог: 401 пост (было 452), 17 страниц списка.

- **2026-08-24 (2)** — Залита реальная выгрузка канала «Доктор Чащина»
  (`ChatExport_2026-08-23 (3)`): 452 поста 2022–2026, 180 webp-фото (15 МБ).
  Список блога пагинирован: `src/pages/blog/[...page].astro` вместо
  `index.astro`, по 24 поста, `/blog/` → `/blog/2/`…`/blog/19/`; пагинатор —
  лаймовые чипы-стрелки как у карусели поста + mono-счётчик «Страница N из M».
  Rest-маршрут с `[slug]` не конфликтует (слуги текстовые). Репозиторий
  подключён к origin (ветка claude/telegram-channel-blog-eci7rn).
  ВАЖНО: перед `npm run tg` нужен `npm install` — без sharp фото копируются
  исходниками. Старые dev-серверы из `Documents\chashina-site` занимали порт
  4331 — та копия проекта больше не рабочая, серверы убиты.

- **2026-08-24** — Блог из телеграм-канала: вкладка `/blog/` (список карточек)
  и страницы постов `/blog/<slug>/` с каруселью фото; «Блог» добавлен в шапку
  и футер, все якоря навигации переведены на `/#…`. Футер вынесен в
  `components/Footer.astro`. Написан импортёр `npm run tg`
  (scripts/import-telegram.mjs): фильтрует выгрузку Telegram Desktop
  (без опросов, видео и коротких анонсов), собирает альбомы в карусели,
  жмёт фото в webp. Выгрузки канала ещё нет — в `src/data/telegram-posts.json`
  лежит помеченная рыба. Подробности — раздел «Блог из телеграм-канала».

- **2026-08-07 (ночь, 3)** — Видео пережаты: ripple 2327→935 КБ, serum
  1898→720 КБ (−60%). Выброшена звуковая дорожка — ролики играют в mute,
  aac просто ехал по сети. Добавлены постеры (960px webp, 22 и 16 КБ):
  до загрузки видео в скруглённой плашке зияла пустота. Рецепт:
  `ffmpeg -threads 1 -i in.mp4 -an -c:v libx264 -crf 32 -preset slow
  -x264-params "threads=1:lookahead-threads=1:rc-lookahead=25:sliced-threads=0"
  -profile:v high -pix_fmt yuv420p -movflags +faststart out.mp4`.
  **`-threads 1` обязателен**: в многопоточном режиме x264 падает на этой
  машине с `malloc of size ... failed`. **VP9/webm не делать** — пробовали
  (`crf 38 -b:v 0`), вышло ТЯЖЕЛЕЕ h264 (1349 против 935 КБ) на этом контенте.

- **2026-08-07 (ночь, 2)** — Убрана секция «Принципы / Что для меня важно»
  целиком (данные, разметка, стили) и бирка «Подход в одной фразе» у манифеста:
  фраза сама себе заголовок. Ссылок на «Принципы» в шапке и футере не было —
  убирать из навигации нечего.

- **2026-08-07 (ночь)** — Перенесён пайплайн выгрузки текстов из `ktm-site`:
  `npm run texts` собирает весь видимый текст страницы в один документ
  `docs/тексты-сайта.md` (17 разделов, ~1400 слов) и его HTML-двойник для
  Google Docs. Отличия от ktm-версии — одностраничник вместо файла на страницу,
  разделы называются «тег + заголовок» с номером, ступени и карточки слайдера
  приклеиваются к своей секции, таймлайн CV и пресса разбираются в таблицы.
  Документ залит в Drive («Оксана Чащина — тексты сайта»), сверка `verify-gdoc.mjs`
  чистая: 184 фрагмента, пропаж 0. Правила — раздел «Тексты сайта на сверку».

- **2026-08-07 (вечер)** — Съёмка в клинике «Атлас» (52 кадра) разложена по сайту
  и собрана в галерею. Исходники — `photos-src/chashina-atlas/` (вне сборки),
  дериваты делает `scripts/build-photos.ps1`: `public/photos/clinic/` 900px для
  плиток и врезок, `clinic/lg/` 1440px для лайтбокса. Кадры с крупными
  логотипами Belotero не берём.
  **Новая секция «Галерея»** между «Обо мне» и «Атласом»: без тега и заголовка,
  только кадры. Десктоп — мозаика edge-to-edge (вертикаль 2 ряда, горизонталь 1),
  телефон — горизонтальная лента 400px с градиентом справа, который гаснет
  в конце прокрутки. Лайтбокс свой, без библиотек: стрелки, Esc, свайп, счётчик.
  **Врезки:** студийный портрет вместо аватарки в «Обо мне», полоса 21:9
  с процедурой в «Инъекционных методах», лайфстайл-кадр в «Домашнем уходе».
  Съёмки НЕ мешаем подряд: клиническая держит рабочие секции, лайфстайл —
  бытовые; в галерее обе, но блоками.
  Спека: `docs/superpowers/specs/2026-08-07-gallery-and-photos-design.md`.

- **2026-08-07** — Мобильная адаптация (пройдена вся страница на 390px):
  хиро на ≤900px больше не скролл-сцена — статичная компоновка (имя лесенкой,
  фото, текст ниже; JS сцены отключён через matchMedia); у свайп-слайдера
  методов спрятан системный скроллбар. Остальные секции легли без правок.
  Пользователь параллельно добавил: блок 10 «Домашний уход» (`.care`, NAOS)
  и секцию «Где я принимаю» (`.clinic`, клиника «Атлас», телефоны/адреса,
  `#contact`) ВМЕСТО формы записи — формы на сайте больше нет, все CTA ведут
  на контакты клиники. Написан этот CLAUDE.md.

- **2026-08-05** — Слайдер методов: убраны номера-чипы; фото-плашки получили
  «стеклянный» низ (размытая копия + маска). Сгенерированы и подключены 4
  иллюстрации методов (art/method-01..04.webp). Манифест перенесён выше — между
  «Как строится работа» и слайдером. Добавлены блоки: Зоны (8), Соцсети (12),
  Обо мне (14, с details-квалификацией), Запись (17, форма `#contact`).
  Футер: statement переписан, «Обо мне» в навигации. Создан PROMPTS.md.
  Отступы: `.mscroll` получил отрицательные margin-block (компенсация пустоты
  100svh-сцены), манифест — `section--tight`. НЕ ЗАКРЫТО: большие внутренние
  отступы тёмных плашек (SMAS/соцсети/запись) — жалоба «много пространства
  снизу и вверху».
- **2026-08-04 (вечер)** — Хиро переделан в светлый без плашки: фото по центру,
  имя выезжает с боков по скроллу, снизу проявляется текст. Фон — canvas-молекулы
  (сетку и светящийся круг пробовали — убрали). Шапка: логотип-плашка удалена,
  меню — глассморфизм. Секция «С чем приходят» удалена целиком (после трёх
  итераций дизайна). «Две половины» — без плашек-карточек, текст колонок ~22px.
  «Что для меня важно» — редакционные строки вместо белых карточек.
  Эксперименты с фото (лента/галерея/вставки в блоки) — всё убрано по просьбе.
  Аппаратные методы превращены в горизонтальный скролл-слайдер, добавлены
  SMAS (ripple.mp4), Инъекции, «Не с крема» (serum.mp4), Пресса. Favicon.
- **2026-08-04 (день)** — Проект переведён с рыбы «Доктор Ливанова» на реальный
  контент Оксаны Чащиной (Тексты_сайта_Чащина.md). Собраны блоки 1–4 + футер.
  Marquee-бегущая строка удалена (не возвращать).
